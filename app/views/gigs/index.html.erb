<h1>List of all gigs taking place at 2016 Edinburgh Festival</h1>

<% @gigs.order(:name).each do |gig| %>
  <p><%= link_to("#{gig.name}", gig_path(gig)) %> 
  <% if can? :create, Gig %> 
    <%= link_to('(Edit)', edit_gig_path(gig)) %>
  <% end %>
  </p>
<% end %>
